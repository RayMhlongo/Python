{
  "name": "phathu-ray-pyte-room",
  "version": "1.0.0",
  "private": true,
  "description": "Shared real-time Python practice room with Socket.io, session-based scoring, and a browser Python IDE.",
  "main": "server/index.js",
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "check": "node --check server/index.js && node --check server/room-manager.js && node --check server/room-challenges.js && node --check assets/app.js && node --check assets/practice-page.js && node --check assets/solo-page.js && node --check assets/room-page.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
