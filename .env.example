PORT=3000

# SQLite persistence path. Relative paths resolve from the project root.
# Local default:
SQLITE_PATH=./data/pyte-room.sqlite

# Railway example when using a mounted volume:
# SQLITE_PATH=/data/pyte-room.sqlite
